SELECT nazwa, ilosc, opis, cena, zdjecie FROM Produkty where Rodzaje_id=1 or Rodzaje_id=2;

SELECT produkty.nazwa, producenci.nazwa from produkty JOIN producenci ON produkty.Producenci_id=producenci.id where produkty.ilosc<10 and produkty.Rodzaje_id=1;

CREATE USER 'Anna'@'localhost' IDENTIFIED BY '@nna2';

INSERT INTO produkty(rodzaje_id, producenci_id, nazwa, ilosc, opis, cena, zdjecie) values( (SELECT id from rodzaje where nazwa='owoce'), (SELECT id FROM producenci where nazwa='warzywa-rolnik'), 'owoc1', 10, '', 9.99, 'owoce.jpg');